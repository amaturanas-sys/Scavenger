# SCAVENGER - dependencias del backend
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
pydantic>=2.6
pulp>=2.8
httpx>=0.27
# Postgres (backend alojado; opcional en local, donde se usa SQLite)
psycopg2-binary>=2.9
# Desarrollo / pruebas
pytest>=8.0
